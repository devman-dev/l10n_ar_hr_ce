<odoo>
    <record id="ir_cron_actualizar_antiguedad_contratos" model="ir.cron">
        <field name="name">Actualizar Antigüedad y Porcentaje Contratos</field>
        <field name="model_id" ref="payroll.model_hr_contract"/>
        <field name="state">code</field>
        <field name="code">model.cron_actualizar_antiguedad()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active">True</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(hour=9, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')" />
    </record>
</odoo>